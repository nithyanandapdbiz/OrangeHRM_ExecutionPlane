apiVersion: v1
kind: Service
metadata:
  name: {{ include "execution-plane.fullname" . }}
  labels:
    {{- include "execution-plane.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "execution-plane.selectorLabels" . | nindent 4 }}
