apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "execution-plane.fullname" . }}-config
  labels:
    {{- include "execution-plane.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
